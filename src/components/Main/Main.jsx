import NewsCard from "../NewsCard/NewsCard";
import "./Main.css";

function Main({ handleCardClick, handleCardLike }) {
  return (
    <main>
      <section className="cards">
        <p className="cards__text">
          
        </p>
        <ul className="cards__list">
        
        </ul>
      </section>
    </main>
  );
}

export default Main;